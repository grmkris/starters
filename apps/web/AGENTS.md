# Web app boundary

Compose routes, providers, DOM UI, and browser adapters here. Shared controls come from `@agent-native/ui`; realtime state belongs in an external store; per-frame scene updates belong in `@agent-native/game-three`. Inspect the rendered route and browser console after visible changes.
