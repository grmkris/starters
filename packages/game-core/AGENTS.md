# Simulation boundary

This package must remain headless and deterministic. It may use Koota and pure TypeScript only. No Effect, React, Three, DOM, Bun, timers, networking, persistence, or wallet code. Fixed-step systems mutate traits; snapshots are produced outside the hot loop.
